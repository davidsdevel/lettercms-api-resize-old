image: ruby:2.6

stages:
  - deploy

staging:
  stage: deploy
  script:
    - gem install dpl
    - dpl --provider=heroku --app=lettercms-resize --api-key=$HEROKU_API_KEY
  environment:
    name: staging
    url: https://lettercms-resize.herokuapp.com
  only:
    - main
